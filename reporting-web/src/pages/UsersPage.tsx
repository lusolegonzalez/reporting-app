import { PageHeader } from '@/components/PageHeader';

export const UsersPage = () => (
  <section>
    <PageHeader title="Usuarios" subtitle="ABM de usuarios (placeholder)." />
    <div className="card">
      <p>Listado y acciones de alta, baja y modificación de usuarios.</p>
    </div>
  </section>
);
